import {Router} from "express"
import TiendaService from '../service/tienda-service.js'


const router = Router();
// const svc =  new TiendaService();


router.get('', function (req, res) {
    //lo que quiero qu envie desde donde
})  // Devuelve Objeto